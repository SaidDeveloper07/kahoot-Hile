(() => { const config = { menuName: "SAID", version: "5.0.0", key: "saidbaba", theme: { primary: "#a855f7", secondary: "#7c3aed", accent: "#c084fc", background: "linear-gradient(135deg, #1a0b2e 0%, #2d1b4e 50%, #1a0b2e 100%)", cardBg: "rgba(168, 85, 247, 0.15)", textColor: "#ffffff", textSecondary: "#c084fc", success: "#22c55e", danger: "#ef4444", warning: "#f59e0b" } }; const styles = `@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap'); * { margin: 0; padding: 0; box-sizing: border-box; } .said-menu-container { font-family: 'Poppins', sans-serif; position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%) scale(0.9); width: 95%; max-width: 1200px; height: 90%; max-height: 700px; background: ${config.theme.background}; border-radius: 20px; box-shadow: 0 25px 50px rgba(0,0,0,0.7), 0 0 50px rgba(168, 85, 247, 0.3); z-index: 999999; opacity: 0; transition: all 0.3s ease-in-out; overflow: hidden; border: 2px solid ${config.theme.primary}; display: none; } .said-menu-container.show { display: flex; flex-direction: column; opacity: 1; transform: translate(-50%, -50%) scale(1); } .said-menu-header { background: linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary}, ${config.theme.accent}); padding: 20px; display: flex; justify-content: space-between; align-items: center; border-bottom: 3px solid rgba(255,255,255,0.2); cursor: move; position: relative; } .said-menu-title { font-size: 28px; font-weight: 800; color: white; text-shadow: 2px 2px 4px rgba(0,0,0,0.5); letter-spacing: 2px; } .said-menu-close { background: rgba(255,255,255,0.2); border: none; color: white; font-size: 24px; width: 45px; height: 45px; border-radius: 50%; cursor: pointer; transition: all 0.3s; display: flex; align-items: center; justify-content: center; } .said-menu-close:hover { background: rgba(255,255,255,0.4); transform: rotate(90deg); } .said-menu-content { display: flex; flex: 1; overflow: hidden; } .said-menu-sidebar { width: 220px; background: rgba(0,0,0,0.4); padding: 15px 0; overflow-y: auto; border-right: 2px solid rgba(168, 85, 247, 0.3); } .said-menu-category { padding: 15px 20px; cursor: pointer; transition: all 0.3s; color: ${config.theme.textSecondary}; font-weight: 600; display: flex; align-items: center; gap: 10px; border-left: 4px solid transparent; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; } .said-menu-category:hover { background: rgba(168, 85, 247, 0.2); color: white; border-left-color: ${config.theme.primary}; } .said-menu-category.active { background: rgba(168, 85, 247, 0.3); color: white; border-left-color: ${config.theme.accent}; box-shadow: inset 0 0 20px rgba(168, 85, 247, 0.2); } .said-menu-cheats { flex: 1; padding: 20px; overflow-y: auto; display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 15px; align-content: start; } .cheat-card { background: ${config.theme.cardBg}; backdrop-filter: blur(10px); border-radius: 15px; padding: 20px; transition: all 0.3s; border: 1px solid rgba(168, 85, 247, 0.3); cursor: pointer; position: relative; min-height: 100px; box-shadow: 0 5px 15px rgba(0,0,0,0.3); } .cheat-card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(168, 85, 247, 0.4); border-color: ${config.theme.accent}; } .cheat-card.active { background: rgba(34, 197, 94, 0.3); border-color: ${config.theme.success}; box-shadow: 0 0 20px rgba(34, 197, 94, 0.4); } .cheat-card-title { font-weight: 700; color: white; margin-bottom: 10px; font-size: 16px; text-transform: uppercase; letter-spacing: 1px; } .cheat-card-description { font-size: 12px; color: ${config.theme.textSecondary}; line-height: 1.4; } .cheat-toggle { position: absolute; top: 15px; right: 15px; width: 45px; height: 22px; background: ${config.theme.danger}; border-radius: 11px; transition: all 0.3s; cursor: pointer; } .cheat-toggle.active { background: ${config.theme.success}; } .cheat-toggle::after { content: ''; position: absolute; top: 3px; left: 3px; width: 16px; height: 16px; background: white; border-radius: 50%; transition: all 0.3s; } .cheat-toggle.active::after { left: 26px; } .cheat-input { width: 100%; padding: 10px; background: rgba(0,0,0,0.4); border: 1px solid rgba(168, 85, 247, 0.4); border-radius: 8px; color: white; font-family: 'Poppins', sans-serif; font-size: 12px; margin-top: 10px; transition: all 0.3s; } .cheat-input:focus { outline: none; border-color: ${config.theme.accent}; box-shadow: 0 0 10px rgba(168, 85, 247, 0.3); } .cheat-slider { width: 100%; margin-top: 10px; -webkit-appearance: none; height: 6px; border-radius: 3px; background: rgba(168, 85, 247, 0.3); outline: none; } .cheat-slider::-webkit-slider-thumb { -webkit-appearance: none; width: 18px; height: 18px; border-radius: 50%; background: ${config.theme.accent}; cursor: pointer; } .said-key-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 9999999; display: flex; align-items: center; justify-content: center; backdrop-filter: blur(10px); } .said-key-box { background: ${config.theme.background}; border-radius: 20px; padding: 40px; box-shadow: 0 0 50px rgba(168, 85, 247, 0.5); border: 2px solid ${config.theme.primary}; text-align: center; max-width: 400px; width: 90%; } .said-key-title { font-size: 32px; font-weight: 800; color: white; margin-bottom: 10px; text-shadow: 2px 2px 4px rgba(0,0,0,0.5); } .said-key-subtitle { font-size: 14px; color: ${config.theme.textSecondary}; margin-bottom: 30px; } .said-key-input { width: 100%; padding: 15px; background: rgba(0,0,0,0.4); border: 2px solid rgba(168, 85, 247, 0.4); border-radius: 10px; color: white; font-family: 'Poppins', sans-serif; font-size: 16px; text-align: center; margin-bottom: 20px; transition: all 0.3s; } .said-key-input:focus { outline: none; border-color: ${config.theme.accent}; box-shadow: 0 0 20px rgba(168, 85, 247, 0.3); } .said-key-button { width: 100%; padding: 15px; background: linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary}); border: none; border-radius: 10px; color: white; font-family: 'Poppins', sans-serif; font-size: 18px; font-weight: 700; cursor: pointer; transition: all 0.3s; text-transform: uppercase; letter-spacing: 2px; } .said-key-button:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(168, 85, 247, 0.4); } .said-floating-button { position: fixed; bottom: 30px; right: 30px; width: 65px; height: 65px; background: linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary}); border-radius: 50%; display: flex; align-items: center; justify-content: center; cursor: pointer; z-index: 999998; box-shadow: 0 10px 25px rgba(168, 85, 247, 0.5); transition: all 0.3s; user-select: none; touch-action: none; animation: pulse 2s infinite; } .said-floating-button:hover { transform: scale(1.1); box-shadow: 0 15px 35px rgba(168, 85, 247, 0.7); } .said-floating-button:active { transform: scale(0.95); } .said-floating-icon { font-size: 32px; color: white; pointer-events: none; } .said-notification { position: fixed; top: 20px; right: 20px; background: linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary}); color: white; padding: 15px 25px; border-radius: 10px; box-shadow: 0 10px 25px rgba(0,0,0,0.5); z-index: 9999999; animation: slideIn 0.3s ease-out; font-family: 'Poppins', sans-serif; font-size: 14px; max-width: 350px; border-left: 4px solid ${config.theme.accent}; } @keyframes pulse { 0% { box-shadow: 0 10px 25px rgba(168, 85, 247, 0.5); } 50% { box-shadow: 0 10px 35px rgba(168, 85, 247, 0.8); } 100% { box-shadow: 0 10px 25px rgba(168, 85, 247, 0.5); } } @keyframes slideIn { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(0); opacity: 1; } } @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } } @media (max-width: 768px) { .said-menu-container { width: 100%; height: 100%; border-radius: 0; } .said-menu-sidebar { width: 100px; } .said-menu-category { font-size: 10px; padding: 10px; } .said-menu-cheats { grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); padding: 10px; gap: 8px; } .cheat-card { padding: 10px; min-height: 70px; } .cheat-card-title { font-size: 12px; } .cheat-card-description { font-size: 9px; } .said-floating-button { width: 50px; height: 50px; bottom: 20px; right: 20px; } .said-floating-icon { font-size: 25px; } .said-menu-title { font-size: 20px; } }`; const styleSheet = document.createElement("style"); styleSheet.textContent = styles; document.head.appendChild(styleSheet); function showNotification(message, type = 'info') { const notification = document.createElement('div'); notification.className = 'said-notification'; notification.style.background = type === 'success' ? `linear-gradient(135deg, ${config.theme.success}, #16a34a)` : type === 'error' ? `linear-gradient(135deg, ${config.theme.danger}, #dc2626)` : `linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary})`; notification.textContent = message; document.body.appendChild(notification); setTimeout(() => { notification.style.opacity = '0'; notification.style.transition = 'opacity 0.3s'; setTimeout(() => notification.remove(), 300); }, 2500); } function createKeySystem() { const overlay = document.createElement('div'); overlay.className = 'said-key-overlay'; overlay.id = 'saidKeyOverlay'; overlay.innerHTML = `<div class="said-key-box"><div class="said-key-title">🔐 SAID</div><div class="said-key-subtitle">Gelişmiş Mod Menü - Key Gerekli</div><input type="password" class="said-key-input" id="saidKeyInput" placeholder="Key girin..."><button class="said-key-button" id="saidKeyButton">Giriş Yap</button></div>`; document.body.appendChild(overlay); const input = document.getElementById('saidKeyInput'); const button = document.getElementById('saidKeyButton'); function checkKey() { const enteredKey = input.value.trim(); if (enteredKey === config.key) { overlay.remove(); showNotification('✅ Key doğru! Mod menü açılıyor...', 'success'); setTimeout(() => { createFloatingButton(); createMenu(); toggleMenu(true); startESP(); showNotification('🎮 SAID Mod Menü yüklendi!', 'success'); }, 500); } else { showNotification('❌ Yanlış key! Tekrar deneyin.', 'error'); input.value = ''; input.style.borderColor = config.theme.danger; input.style.boxShadow = '0 0 20px rgba(239, 68, 68, 0.5)'; setTimeout(() => { input.style.borderColor = 'rgba(168, 85, 247, 0.4)'; input.style.boxShadow = 'none'; }, 1000); } } button.addEventListener('click', checkKey); input.addEventListener('keypress', (e) => { if (e.key === 'Enter') checkKey(); }); input.focus(); } let espEnabled = false; let espBoxes = []; let espInterval = null; function startESP() { espInterval = setInterval(() => { if (!espEnabled) { espBoxes.forEach(box => box.remove()); espBoxes = []; return; } try { const canvas = document.querySelector('canvas'); if (!canvas) return; const ctx = canvas.getContext('webgl') || canvas.getContext('webgl2'); if (!ctx) return; const gameInstance = window.game || window.Game || window.kirka || window.Kirka; if (!gameInstance) return; const players = gameInstance.players || gameInstance.game?.players || gameInstance.state?.players || []; const localPlayer = gameInstance.player || gameInstance.localPlayer || gameInstance.self; if (!localPlayer) return; espBoxes.forEach(box => box.remove()); espBoxes = []; const camera = gameInstance.camera || gameInstance.game?.camera || gameInstance.renderer?.camera; if (!camera) return; players.forEach(player => { if (player === localPlayer || !player.alive || !player.position) return; const pos = player.position; const screenPos = worldToScreen(pos, camera, canvas); if (!screenPos) return; const box = document.createElement('div'); box.className = 'said-esp-box'; box.style.left = screenPos.x + 'px'; box.style.top = screenPos.y + 'px'; box.style.width = screenPos.width + 'px'; box.style.height = screenPos.height + 'px'; if (player.health !== undefined) { const healthBar = document.createElement('div'); healthBar.className = 'said-esp-health'; healthBar.style.width = (player.health / 100) * 100 + '%'; healthBar.style.background = player.health > 50 ? '#22c55e' : player.health > 25 ? '#f59e0b' : '#ef4444'; box.appendChild(healthBar); } if (player.name) { const nameTag = document.createElement('div'); nameTag.className = 'said-esp-name'; nameTag.textContent = player.name; box.appendChild(nameTag); } document.body.appendChild(box); espBoxes.push(box); }); } catch (e) {} }, 16); } function worldToScreen(position, camera, canvas) { try { if (!position || !camera) return null; const dx = position.x - (camera.position?.x || camera.x || 0); const dy = position.y - (camera.position?.y || camera.y || 0); const dz = position.z - (camera.position?.z || camera.z || 0); if (dz > -1) return null; const fov = camera.fov || 75; const aspect = canvas.width / canvas.height; const fovRad = (fov * Math.PI) / 180; const tanFov = Math.tan(fovRad / 2); const screenX = (dx / dz) / tanFov / aspect; const screenY = (dy / dz) / tanFov; if (Math.abs(screenX) > 1 || Math.abs(screenY) > 1) return null; const width = canvas.width; const height = canvas.height; const x = (screenX + 1) * width / 2; const y = (1 - screenY) * height / 2; const playerHeight = 2; const playerWidth = 0.6; const scale = (playerHeight / -dz) * (height / 2) / tanFov; const boxWidth = playerWidth * scale; const boxHeight = playerHeight * scale; return { x: x - boxWidth / 2, y: y - boxHeight, width: boxWidth, height: boxHeight }; } catch (e) { return null; } } const cheats = { "🎯 Aimbot": [ { name: "Aimbot", description: "Otomatik nişan alma sistemi", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Aimbot AÇIK" : "Aimbot KAPALI", this.enabled ? "success" : "error"); if (this.enabled) startAimbot(); } }, { name: "FOV Ayarı", description: "Aimbot görüş alanını ayarla", type: "slider", value: "90", min: "10", max: "360", run: function(value) { aimbotFOV = parseInt(value); showNotification(`FOV: ${value}°`, "info"); } }, { name: "Magic Bullet", description: "Mermiler hedefe otomatik gider", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Magic Bullet AÇIK" : "Magic Bullet KAPALI", this.enabled ? "success" : "error"); } }, { name: "Bullet Track", description: "Mermi takip sistemi", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Bullet Track AÇIK" : "Bullet Track KAPALI", this.enabled ? "success" : "error"); } }, { name: "Smooth Aim", description: "Yumuşak nişan alma", type: "slider", value: "5", min: "1", max: "20", run: function(value) { aimbotSmooth = parseInt(value); showNotification(`Smooth: ${value}`, "info"); } }, { name: "Hedef Kemik", description: "Nişan alınacak bölge", type: "input", value: "head", run: function(value) { aimbotBone = value.toLowerCase(); showNotification(`Hedef: ${value}`, "info"); } }, { name: "Nereye Sıkarsan", description: "Nereye sıkarsan hedefe gider", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Serbest vuruş AÇIK" : "Serbest vuruş KAPALI", this.enabled ? "success" : "error"); } } ], "👁️ ESP": [ { name: "Oyuncu ESP", description: "Oyuncuları duvar arkasından gör", type: "toggle", enabled: false, run: function() { espEnabled = this.enabled; showNotification(this.enabled ? "ESP AÇIK" : "ESP KAPALI", this.enabled ? "success" : "error"); if (!this.enabled) { espBoxes.forEach(box => box.remove()); espBoxes = []; } } }, { name: "Can Göstergesi", description: "Oyuncuların canını göster", type: "toggle", enabled: true, run: function() { showNotification(this.enabled ? "Can göstergesi AÇIK" : "Can göstergesi KAPALI", this.enabled ? "success" : "error"); } }, { name: "İsim Göster", description: "Oyuncu isimlerini göster", type: "toggle", enabled: true, run: function() { showNotification(this.enabled ? "İsimler AÇIK" : "İsimler KAPALI", this.enabled ? "success" : "error"); } }, { name: "ESP Renk", description: "ESP kutusunun rengini değiştir", type: "input", value: "#a855f7", run: function(value) { espColor = value; showNotification(`ESP rengi: ${value}`, "info"); } }, { name: "ESP Boyut", description: "ESP kutusu boyutunu ayarla", type: "slider", value: "1", min: "0.5", max: "2", run: function(value) { espScale = parseFloat(value); showNotification(`ESP boyutu: ${value}x`, "info"); } }, { name: "Canlı Konum", description: "Oyuncuların canlı konumunu göster", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Canlı konum AÇIK" : "Canlı konum KAPALI", this.enabled ? "success" : "error"); } } ], "🎨 Skin Changer": [ { name: "Skin Changer", description: "Silah skinlerini değiştir", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Skin Changer AÇIK" : "Skin Changer KAPALI", this.enabled ? "success" : "error"); } }, { name: "AK-47 Skin", description: "AK-47 için skin seç", type: "input", value: "Dragon Lore", run: function(value) { showNotification(`AK-47: ${value}`, "info"); } }, { name: "M4A1 Skin", description: "M4A1 için skin seç", type: "input", value: "Howl", run: function(value) { showNotification(`M4A1: ${value}`, "info"); } }, { name: "AWP Skin", description: "AWP için skin seç", type: "input", value: "Medusa", run: function(value) { showNotification(`AWP: ${value}`, "info"); } }, { name: "Deagle Skin", description: "Deagle için skin seç", type: "input", value: "Blaze", run: function(value) { showNotification(`Deagle: ${value}`, "info"); } }, { name: "Bıçak Skin", description: "Bıçak skinini değiştir", type: "input", value: "Karambit", run: function(value) { showNotification(`Bıçak: ${value}`, "info"); } }, { name: "Eldiven Skin", description: "Eldiven skinini değiştir", type: "input", value: "Sport Gloves", run: function(value) { showNotification(`Eldiven: ${value}`, "info"); } } ], "⚡ Hızlı Özellikler": [ { name: "Hızlı Ateş", description: "Silahı daha hızlı ateşle", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Hızlı ateş AÇIK" : "Hızlı ateş KAPALI", this.enabled ? "success" : "error"); } }, { name: "Sonsuz Mermi", description: "Mermin hiç bitmez", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Sonsuz mermi AÇIK" : "Sonsuz mermi KAPALI", this.enabled ? "success" : "error"); } }, { name: "Geri Tepme Yok", description: "Silah geri tepmesini kaldır", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Geri tepme yok AÇIK" : "Geri tepme yok KAPALI", this.enabled ? "success" : "error"); } }, { name: "Hızlı Şarjör", description: "Anında şarjör değiştir", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Hızlı şarjör AÇIK" : "Hızlı şarjör KAPALI", this.enabled ? "success" : "error"); } }, { name: "Hızlı Koşu", description: "Daha hızlı koş", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Hızlı koşu AÇIK" : "Hızlı koşu KAPALI", this.enabled ? "success" : "error"); } }, { name: "Yüksek Zıplama", description: "Daha yükseğe zıpla", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Yüksek zıplama AÇIK" : "Yüksek zıplama KAPALI", this.enabled ? "success" : "error"); } } ], "🛡️ Savunma": [ { name: "Sonsuz Can", description: "Canın hiç bitmez", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Sonsuz can AÇIK" : "Sonsuz can KAPALI", this.enabled ? "success" : "error"); } }, { name: "Ölümsüzlük", description: "Asla ölme", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Ölümsüzlük AÇIK" : "Ölümsüzlük KAPALI", this.enabled ? "success" : "error"); } }, { name: "Duvar Hack", description: "Duvarların içinden geç", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Duvar hack AÇIK" : "Duvar hack KAPALI", this.enabled ? "success" : "error"); if (this.enabled) { enableWallHack(); } } }, { name: "Görünmezlik", description: "Düşmanlar seni göremez", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Görünmezlik AÇIK" : "Görünmezlik KAPALI", this.enabled ? "success" : "error"); } }, { name: "Hızlı İyileşme", description: "Daha hızlı iyileş", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Hızlı iyileşme AÇIK" : "Hızlı iyileşme KAPALI", this.enabled ? "success" : "error"); } } ], "📊 Radar": [ { name: "Radar Hack", description: "Mini haritada herkesi gör", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Radar hack AÇIK" : "Radar hack KAPALI", this.enabled ? "success" : "error"); } }, { name: "Radar Boyutu", description: "Radar boyutunu ayarla", type: "slider", value: "100", min: "50", max: "300", run: function(value) { showNotification(`Radar boyutu: ${value}px`, "info"); } }, { name: "Radar Şeffaflık", description: "Radar şeffaflığını ayarla", type: "slider", value: "80", min: "10", max: "100", run: function(value) { showNotification(`Şeffaflık: %${value}`, "info"); } } ], "⚙️ Ayarlar": [ { name: "Menü Şeffaflığı", description: "Menü şeffaflığını ayarla", type: "slider", value: "95", min: "50", max: "100", run: function(value) { showNotification(`Şeffaflık: %${value}`, "info"); } }, { name: "Menü Boyutu", description: "Menü boyutunu ayarla", type: "slider", value: "100", min: "50", max: "150", run: function(value) { showNotification(`Boyut: %${value}`, "info"); } }, { name: "Bildirimler", description: "Bildirimleri aç/kapat", type: "toggle", enabled: true, run: function() { showNotification(this.enabled ? "Bildirimler AÇIK" : "Bildirimler KAPALI", this.enabled ? "success" : "error"); } }, { name: "Esp Kutusu Rengi", description: "ESP kutusunun rengini değiştir", type: "input", value: "#a855f7", run: function(value) { espColor = value; showNotification(`Renk: ${value}`, "info"); } } ] }; let espColor = "#a855f7"; let espScale = 1; let aimbotFOV = 90; let aimbotSmooth = 5; let aimbotBone = "head"; let aimbotInterval = null; function startAimbot() { if (aimbotInterval) { clearInterval(aimbotInterval); aimbotInterval = null; return; } aimbotInterval = setInterval(() => { try { const gameInstance = window.game || window.Game || window.kirka || window.Kirka; if (!gameInstance) return; const players = gameInstance.players || gameInstance.game?.players || gameInstance.state?.players || []; const localPlayer = gameInstance.player || gameInstance.localPlayer || gameInstance.self; if (!localPlayer || !localPlayer.aimDirection || !localPlayer.position) return; let closestPlayer = null; let closestDistance = Infinity; players.forEach(player => { if (player === localPlayer || !player.alive || !player.position) return; const dx = player.position.x - localPlayer.position.x; const dy = player.position.y - localPlayer.position.y; const dz = player.position.z - localPlayer.position.z; const distance = Math.sqrt(dx*dx + dy*dy + dz*dz); if (distance < closestDistance) { closestDistance = distance; closestPlayer = player; } }); if (closestPlayer) { const targetX = closestPlayer.position.x; const targetY = closestPlayer.position.y + (aimbotBone === "head" ? 1.7 : aimbotBone === "body" ? 0.9 : 1.2); const targetZ = closestPlayer.position.z; const dx = targetX - localPlayer.position.x; const dy = targetY - localPlayer.position.y; const dz = targetZ - localPlayer.position.z; const distance = Math.sqrt(dx*dx + dy*dy + dz*dz); const yaw = Math.atan2(dx, dz); const pitch = Math.atan2(dy, Math.sqrt(dx*dx + dz*dz)); localPlayer.aimDirection.x = yaw; localPlayer.aimDirection.y = pitch; localPlayer.rotation = { x: pitch, y: yaw }; } } catch (e) {} }, 16); } function enableWallHack() { try { const gameInstance = window.game || window.Game || window.kirka || window.Kirka; if (!gameInstance) return; const world = gameInstance.world || gameInstance.game?.world || gameInstance.scene; if (!world) return; const objects = world.objects || world.children || world.meshes || []; objects.forEach(obj => { if (obj && obj.material && obj.material.wireframe !== undefined) { obj.material.wireframe = true; obj.material.transparent = true; obj.material.opacity = 0.3; } }); } catch (e) {} } let menuElement = null; let isMenuOpen = false; let floatingButton = null; function createFloatingButton() { floatingButton = document.createElement('div'); floatingButton.className = 'said-floating-button'; floatingButton.innerHTML = '<span class="said-floating-icon">🎮</span>'; let isDragging = false; let startX, startY, startLeft, startTop; floatingButton.addEventListener('mousedown', (e) => { isDragging = true; startX = e.clientX; startY = e.clientY; const rect = floatingButton.getBoundingClientRect(); startLeft = rect.left; startTop = rect.top; floatingButton.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('mousemove', (e) => { if (isDragging) { const dx = e.clientX - startX; const dy = e.clientY - startY; floatingButton.style.left = startLeft + dx + 'px'; floatingButton.style.top = startTop + dy + 'px'; floatingButton.style.right = 'auto'; floatingButton.style.bottom = 'auto'; } }); document.addEventListener('mouseup', (e) => { if (isDragging) { isDragging = false; floatingButton.style.transition = 'all 0.3s'; const dx = e.clientX - startX; const dy = e.clientY - startY; if (Math.abs(dx) < 5 && Math.abs(dy) < 5) { toggleMenu(); } } }); floatingButton.addEventListener('touchstart', (e) => { isDragging = true; const touch = e.touches[0]; startX = touch.clientX; startY = touch.clientY; const rect = floatingButton.getBoundingClientRect(); startLeft = rect.left; startTop = rect.top; floatingButton.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('touchmove', (e) => { if (isDragging) { const touch = e.touches[0]; const dx = touch.clientX - startX; const dy = touch.clientY - startY; floatingButton.style.left = startLeft + dx + 'px'; floatingButton.style.top = startTop + dy + 'px'; floatingButton.style.right = 'auto'; floatingButton.style.bottom = 'auto'; e.preventDefault(); } }, { passive: false }); document.addEventListener('touchend', (e) => { if (isDragging) { isDragging = false; floatingButton.style.transition = 'all 0.3s'; const touch = e.changedTouches[0]; const dx = touch.clientX - startX; const dy = touch.clientY - startY; if (Math.abs(dx) < 5 && Math.abs(dy) < 5) { toggleMenu(); } } }); document.body.appendChild(floatingButton); } function createMenu() { const container = document.createElement('div'); container.className = 'said-menu-container'; container.id = 'saidMenu'; const header = document.createElement('div'); header.className = 'said-menu-header'; header.innerHTML = `<div class="said-menu-title">🔮 SAID</div><button class="said-menu-close">✕</button>`; const content = document.createElement('div'); content.className = 'said-menu-content'; const sidebar = document.createElement('div'); sidebar.className = 'said-menu-sidebar'; const cheatsArea = document.createElement('div'); cheatsArea.className = 'said-menu-cheats'; let activeCategory = null; Object.keys(cheats).forEach((category, index) => { const categoryBtn = document.createElement('div'); categoryBtn.className = 'said-menu-category'; categoryBtn.textContent = category; categoryBtn.onclick = () => { document.querySelectorAll('.said-menu-category').forEach(c => c.classList.remove('active')); categoryBtn.classList.add('active'); showCheats(category); }; if (index === 0) { categoryBtn.classList.add('active'); activeCategory = category; } sidebar.appendChild(categoryBtn); }); function showCheats(category) { cheatsArea.innerHTML = ''; cheats[category].forEach((cheat, index) => { const card = document.createElement('div'); card.className = 'cheat-card'; card.innerHTML = `<div class="cheat-card-title">${cheat.name}</div><div class="cheat-card-description">${cheat.description}</div>`; if (cheat.type === 'toggle') { const toggle = document.createElement('div'); toggle.className = `cheat-toggle ${cheat.enabled ? 'active' : ''}`; toggle.style.position = 'absolute'; toggle.style.top = '15px'; toggle.style.right = '15px'; card.onclick = () => { cheat.enabled = !cheat.enabled; toggle.classList.toggle('active', cheat.enabled); card.classList.toggle('active', cheat.enabled); cheat.run(); }; card.appendChild(toggle); } else if (cheat.type === 'input') { const input = document.createElement('input'); input.type = 'text'; input.className = 'cheat-input'; input.placeholder = 'Değer girin...'; input.value = cheat.value || ''; input.onclick = (e) => e.stopPropagation(); input.onkeypress = (e) => { if (e.key === 'Enter') { cheat.run(input.value); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); } }; card.onclick = () => { if (document.activeElement !== input) { cheat.run(input.value); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); } }; card.appendChild(input); } else if (cheat.type === 'slider') { const slider = document.createElement('input'); slider.type = 'range'; slider.className = 'cheat-slider'; slider.min = cheat.min || '0'; slider.max = cheat.max || '100'; slider.value = cheat.value || '50'; slider.onclick = (e) => e.stopPropagation(); slider.oninput = (e) => { e.stopPropagation(); }; slider.onchange = (e) => { cheat.run(slider.value); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); }; card.appendChild(slider); } else { card.onclick = () => { cheat.run(); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); }; } card.style.animation = `fadeIn 0.3s ease-out ${index * 30}ms both`; cheatsArea.appendChild(card); }); } showCheats(activeCategory); content.appendChild(sidebar); content.appendChild(cheatsArea); container.appendChild(header); container.appendChild(content); document.body.appendChild(container); header.querySelector('.said-menu-close').onclick = () => { toggleMenu(false); }; let isDraggingMenu = false; let menuStartX, menuStartY, menuStartLeft, menuStartTop; header.addEventListener('mousedown', (e) => { isDraggingMenu = true; menuStartX = e.clientX; menuStartY = e.clientY; const rect = container.getBoundingClientRect(); menuStartLeft = rect.left; menuStartTop = rect.top; container.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('mousemove', (e) => { if (isDraggingMenu) { const dx = e.clientX - menuStartX; const dy = e.clientY - menuStartY; container.style.left = menuStartLeft + dx + 'px'; container.style.top = menuStartTop + dy + 'px'; container.style.transform = 'none'; } }); document.addEventListener('mouseup', () => { if (isDraggingMenu) { isDraggingMenu = false; container.style.transition = 'all 0.3s'; } }); header.addEventListener('touchstart', (e) => { isDraggingMenu = true; const touch = e.touches[0]; menuStartX = touch.clientX; menuStartY = touch.clientY; const rect = container.getBoundingClientRect(); menuStartLeft = rect.left; menuStartTop = rect.top; container.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('touchmove', (e) => { if (isDraggingMenu) { const touch = e.touches[0]; const dx = touch.clientX - menuStartX; const dy = touch.clientY - menuStartY; container.style.left = menuStartLeft + dx + 'px'; container.style.top = menuStartTop + dy + 'px'; container.style.transform = 'none'; e.preventDefault(); } }, { passive: false }); document.addEventListener('touchend', () => { if (isDraggingMenu) { isDraggingMenu = false; container.style.transition = 'all 0.3s'; } }); return container; } function toggleMenu(show) { if (!menuElement) { menuElement = createMenu(); } if (show === undefined) { show = !isMenuOpen; } isMenuOpen = show; if (show) { menuElement.classList.add('show'); if (floatingButton) { floatingButton.style.display = 'none'; } } else { menuElement.classList.remove('show'); if (floatingButton) { floatingButton.style.display = 'flex'; } } } document.addEventListener('keydown', (e) => { if (e.ctrlKey && e.key.toLowerCase() === 'e') { e.preventDefault(); toggleMenu(); } }); function initialize() { console.log('%c🔮 SAID Mod Menü', 'color: #a855f7; font-size: 25px; font-weight: bold;'); console.log('%cVersiyon: ' + config.version, 'color: #c084fc; font-size: 16px;'); console.log('%cKirka.io için gelişmiş ESP sistemi', 'color: #22c55e; font-size: 14px;'); console.log('%cKey: saidbaba', 'color: #f59e0b; font-size: 14px;'); createKeySystem(); } if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initialize); } else { initialize(); } })();
